- name: Deploy S3 Bucket
  run: |
    aws cloudformation deploy \
      --stack-name reddy2026-test-stack \
      --template-file template.yaml \
      --capabilities CAPABILITY_NAMED_IAM \
      --region ap-south-1

